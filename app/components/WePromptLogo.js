export default function WePromptLogo({ id, textColor }) {
  return (
    <img
      src="/logo.png"
      alt="WePrompt"
      style={{ height: 80, width: "auto", display: "block", maxWidth: "none" }}
    />
  );
}