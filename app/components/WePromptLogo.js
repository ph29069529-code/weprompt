export default function WePromptLogo({ id, textColor }) {
  return (
    <img
      src="/logo.png"
      alt="WePrompt"
      style={{ height: 56, width: "auto", display: "block", maxWidth: "none" }}
    />
  );
}